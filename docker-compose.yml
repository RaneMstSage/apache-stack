services:
  # Initial setup services - uncomment to bootstrap configuration
  # apache-init:
  #   build:
  #     context: ./apache
  #     dockerfile: Dockerfile
  #   container_name: apache-init
  #   command: >
  #     bash -c "
  #       mkdir -p /mnt/conf /mnt/htdocs &&
  #       cp -r /usr/local/apache2/conf/* /mnt/conf 2>/dev/null || true &&
  #       cp -r /usr/local/apache2/htdocs/* /mnt/htdocs 2>/dev/null || true"
  #   volumes:
  #     - ~/docker-volumes/apache-stack/conf:/mnt/conf
  #     - ~/docker-volumes/apache-stack/htdocs:/mnt/htdocs
  #   entrypoint: ""

  # php-init:
  #   build:
  #     context: ./php
  #     dockerfile: Dockerfile
  #   container_name: php-init
  #   command: >
  #     bash -c "
  #       mkdir -p /mnt/php-conf &&
  #       cp /usr/local/etc/php/php.ini-production /mnt/php-conf/php.ini 2>/dev/null || true"
  #   volumes:
  #     - ~/docker-volumes/apache-stack/php:/mnt/php-conf
  #   entrypoint: ""

  apache:
    build:
      context: ./apache
      dockerfile: Dockerfile
    container_name: apache
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ~/docker-volumes/apache-stack/conf:/usr/local/apache2/conf
      - ~/docker-volumes/apache-stack/htdocs:/usr/local/apache2/htdocs
      - ~/docker-volumes/apache-stack/var:/usr/local/apache2/var
      - ~/docker-volumes/apache-stack/uploads:/usr/local/apache2/uploads
      - ~/docker-volumes/apache-stack/user.passwd:/usr/local/apache2/user.passwd
      - ~/docker-volumes/apache-stack/python-apps:/usr/local/apache2/python-apps
      - svn_repos:/usr/local/apache2/svn
      - git_repos:/usr/local/apache2/git
    restart: unless-stopped
    networks:
      - apache-network
    depends_on:
      - php

  php:
    build:
      context: ./php
      dockerfile: Dockerfile
    container_name: php-fpm
    volumes:
    # Match Apache's path exactly
      - ~/docker-volumes/apache-stack/htdocs:/usr/local/apache2/htdocs  
      - ~/docker-volumes/apache-stack/php/php.ini:/usr/local/etc/php/php.ini
      - ~/docker-volumes/apache-stack/uploads:/usr/local/apache2/uploads
    expose:
      - "9000"
    restart: unless-stopped
    networks:
      - apache-network

  mysql:
    container_name: mysql
    image: mysql:8.0
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    networks:
      - apache-network

  redmine:
    container_name: redmine
    image: redmine:6.0.5
    restart: always
    ports:
      - "3000:3000"
    environment:
      REDMINE_DB_MYSQL: mysql
      REDMINE_DB_DATABASE: ${REDMINE_DB_DATABASE}
      REDMINE_DB_USERNAME: ${REDMINE_DB_USERNAME}
      REDMINE_DB_PASSWORD: ${REDMINE_DB_PASSWORD}
      REDMINE_SECRET_KEY_BASE: ${REDMINE_SECRET_KEY_BASE}
    volumes:
    - ~/docker-volumes/apache-stack/redmine/files:/usr/src/redmine/files
    - ~/docker-volumes/apache-stack/redmine/plugins:/usr/src/redmine/plugins
    - ~/docker-volumes/apache-stack/redmine/config:/usr/src/redmine/config
    - ~/docker-volumes/apache-stack/redmine/themes:/usr/src/redmine/themes
    networks:
      - apache-network
    depends_on:
      - mysql

  redmine51:
    container_name: redmine51
    image: redmine:5.1
    restart: always
    ports:
      - "3001:3000"
    environment:
      REDMINE_DB_MYSQL: mysql
      REDMINE_DB_DATABASE: sagemine
      REDMINE_DB_USERNAME: ${REDMINE_DB_USERNAME}
      REDMINE_DB_PASSWORD: ${REDMINE_DB_PASSWORD}
      REDMINE_SECRET_KEY_BASE: ${REDMINE_SECRET_KEY_BASE}
    volumes:
    - ~/docker-volumes/apache-stack/redmine51/files:/usr/src/redmine/files
    - ~/docker-volumes/apache-stack/redmine51/plugins:/usr/src/redmine/plugins
    - ~/docker-volumes/apache-stack/redmine51/config:/usr/src/redmine/config
    - ~/docker-volumes/apache-stack/redmine51/themes:/usr/src/redmine/public/themes
    networks:
      - apache-network
    depends_on:
      - mysql

networks:
  apache-network:
    driver: bridge

# Add this volumes section
volumes:
  svn_repos:
  git_repos:
  mysql_data: